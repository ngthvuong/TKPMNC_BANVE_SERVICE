# TKPMNC_BANVE_SERVICE

## Create requirements.txt
pip freeze > requirements.txt

## Recreate The Virtual environment
pip install -r requirements.txt
python -m venv .venv
.venv\Scripts\activate on Windows
